name: razorpay_flutter_template
description: A new Flutter project.
publish_to: "none"
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  razorpay_flutter: ^1.3.4
  flutter_dotenv: ^5.0.2
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/.env
